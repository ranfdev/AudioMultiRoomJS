{
  "name": "AudioMultiRoomJS",
  "version": "1.0.0",
  "description": "Listen your music everywhere in your house",
  "main": "server.js",
  "dependencies": {
    "express": "^4.14.0",
    "open": "0.0.5",
    "opener": "^1.4.2",
    "socket.io": "^1.7.2"
  },
  "devDependencies": {
    "bower": "^1.8.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "postinstall": "npm run build",
    "build": "cd client/ && bower i && echo 'installed'"
  },
  "author": "RanfDev",
  "license": "MIT"
}
